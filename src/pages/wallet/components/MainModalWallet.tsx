import React, { FunctionComponent } from 'react';
import { Modal } from 'antd';

export interface MainModalWalletProps {}

const MainModalWallet: FunctionComponent<{}> = () => {
  return (
    <div>
      <div>Modal </div>
    </div>
  );
};

export default MainModalWallet;
